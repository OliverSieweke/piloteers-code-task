{
    "$schema": "http://json-schema.org/schema#",
    "title": "Location",
    "type": "object",
    "properties": {
        "latitude": {
            "description": "Latitude in degrees. The expected format is a number, not a string with minutes and seconds.",
            "type": "number",
            "minimum": -90,
            "maximum": 90
        },
        "longitude": {
            "description": "Longitude in degrees. The expected format is a number, not a string with minutes and seconds.",
            "type": "number",
            "minimum": -180,
            "maximum": 180
        },
        "additionalData": {}
    },
    "required": [
        "latitude",
        "longitude"
    ]
}
